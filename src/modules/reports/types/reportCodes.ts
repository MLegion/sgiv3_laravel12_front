/**
 * Claves estables de reportes referenciados por otros módulos.
 * Cada clave corresponde al campo `code` en la tabla `reports`.
 * El admin crea el reporte con esta clave y sube la plantilla Word.
 */
export enum ReportCode {
    TEACHER_SUBJECT_REQUEST = 'F0009',
}
