# Vacío = URL relativa (mismo origen que sirve la SPA). Así funciona por
# :8080 (http), por Traefik (https://sgiv3-laravel.itstb.test) y por IP LAN.
VITE_API_URL=
VITE_APP_NAME=SGIv3
VITE_APP_ENV=local
